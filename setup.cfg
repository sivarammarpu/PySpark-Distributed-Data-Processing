[tool:pytest]
testpaths = tests
addopts = -v --tb=short
# Ensure src/ imports work without installing the package
pythonpath = .

[coverage:run]
source = src
branch = True

[coverage:report]
omit =
    */tests/*
    */__pycache__/*
    */site-packages/*
show_missing = True
